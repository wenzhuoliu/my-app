/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  distDir: 'docs',
  basePath: '/zland',
  assetPrefix: 'https://wenzhuoliu.github.io/zland',
};

export default nextConfig;
